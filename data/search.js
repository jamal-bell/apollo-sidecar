import { lessonsData, usersData } from "./index.js";
import validation from "./validation.js"

// const exportedMethods = {

//     searchLessonByName (searchTerm, platform) {
//         if (!searchTerm) throw "You must provide a search term.";
//         if (typeof searchTerm !== "string" || searchTerm.trim().length === 0) throw "Enter a valid search string";
//         if (platform === "lesson") {
//             //do this
//             const listlLesson = await lessonsData.getAllLessons().
//         } else if (platform === "qa") {
//             // do this
//         } else {
//             // search all
//         }
//     }

// }